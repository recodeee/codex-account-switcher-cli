# codex-account-switcher v0.1.18

## Summary
This patch bumps the publish version again and makes every codex-auth update prompt/install path show the exact npm version it will install.

## What's changed
- Updated package metadata from `0.1.17` to `0.1.18`.
- Pinned update installs to the fetched npm version instead of generic `@latest`.
- Updated `list`, `self-update`, and startup update prompts/messages to show the exact target version.

## Verification
- `npm test --silent`
- `npm pack --dry-run`

## Upgrade notes
- No breaking CLI changes.
- Update prompts now tell users which exact release will be installed.

## Publish checklist
1. Review package metadata and release notes.
2. `npm test --silent`
3. `npm pack --dry-run`
4. `npm publish --access public`
