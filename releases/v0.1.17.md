# codex-account-switcher v0.1.17

## Summary
This patch bumps the publish version so the npm package can be released after `0.1.16` was already taken.

## What's changed
- Updated package metadata from `0.1.16` to `0.1.17`.
- Kept the packaged code unchanged from the previous release candidate.
- Added release notes for the republish-only patch release.

## Verification
- `npm test --silent`
- `npm pack --dry-run`

## Upgrade notes
- No breaking CLI changes.
- This release exists to unblock npm publishing after the `0.1.16` version collision.

## Publish checklist
1. Review package metadata and release notes.
2. `npm test --silent`
3. `npm pack --dry-run`
4. `npm publish --access public`
