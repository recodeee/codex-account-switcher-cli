# Storefront app slot

This folder exists to mirror the `WEBU/apps/{backend,storefront}` layout.

Current active storefront code remains in the root `frontend/` app.
When ready, storefront sources can be moved here in a dedicated migration step.
